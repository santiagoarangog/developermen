<!doctype html>
<html lang="en">

<head>
  <!-- Required meta tags -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <link rel="mask-icon" type="" href="https://developermen.com/assets/favicons/android-icon-192x192.png" color="#111" />
  <!-- Android Icons -->
  <link rel="icon" type="image/png" sizes="57x57" href="https://developermen.com/assets/favicons/android-icon-144x144.png" />
  <link rel="icon" type="image/png" sizes="60x60" href="https://developermen.com/assets/favicons/android-icon-192x192.png" />
  <link rel="icon" type="image/png" sizes="76x76" href="https://developermen.com/assets/favicons/android-icon-36x36.png" />
  <link rel="icon" type="image/png" sizes="152x152" href="https://developermen.com/assets/favicons/android-icon-48x48.png" />
  <link rel="icon" type="image/png" sizes="180x180" href="https://developermen.com/assets/favicons/android-icon-72x72.png" />
  <!-- <link rel="icon" type="image/png" sizes="180x180" href="assets/favicons/android-icon-96x96.png"/> -->

  <!-- Apple Icons -->
  <link rel="apple-touch-icon" type="image/png" sizes="114x114" href="https://developermen.com/assets/favicons/apple-icon-114x114.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="120x120" href="https://developermen.com/assets/favicons/apple-icon-120x120.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="144x144" href="https://developermen.com/assets/favicons/apple-icon-144x144.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="152x152" href="https://developermen.com/assets/favicons/apple-icon-152x152.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="180x180" href="https://developermen.com/assets/favicons/apple-icon-180x180.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="57x57" href="https://developermen.com/assets/favicons/apple-icon-57x57.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="72x72" href="https://developermen.com/assets/favicons/apple-icon-72x72.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="76x76" href="favicons/apple-icon-76x76.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="precomposed" href="https://developermen.com/assets/favicons/apple-icon-precomposed.png" />
  <link rel="apple-touch-icon" type="image/png" sizes="auto" href="https://developermen.com/assets/favicons/apple-icon.png" />

  <!-- Favicon -->
  <link rel="shortcut icon" type="image/x-icon" href="https://developermen.com/assets/favicons/favicon-16x16.png" />
  <link rel="icon" type="image/png" href="https://developermen.com/assets/favicons/favicon-16x16.png" sizes="16x16" />
  <link rel="icon" type="image/png" href="https://developermen.com/assets/favicons/favicon-32x32.png" sizes="32x32" />

  <!-- Ms Icon -->
  <link rel="ms-icon" type="image/png" href="https://developermen.com/assets/favicons/ms-icon-70x70.png" sizes="70x70" />
  <link rel="ms-icon" type="image/png" href="https://developermen.com/assets/favicons/ms-icon-144x144.png" sizes="144x144" />
  <link rel="ms-icon" type="image/png" href="https://developermen.com/assets/favicons/ms-icon-150x150.png" sizes="150x150" />
  <link rel="ms-icon" type="image/png" href="https://developermen.com/assets/favicons/ms-icon-310x310.png" sizes="310x310" />
  <link rel="manifest" href="https://developermen.com/assets/favicons/manifest.json" />

  <!-- Libs CSS -->
  <link rel="stylesheet" href="https://developermen.com/assets/fonts/Feather/feather.css">
  <link rel="stylesheet" href="https://developermen.com/assets/libs/flickity/dist/flickity.min.css">
  <link rel="stylesheet" href="https://developermen.com/assets/libs/flickity-fade/flickity-fade.css">
  <link rel="stylesheet" href="https://developermen.com/assets/libs/aos/dist/aos.css">
  <link rel="stylesheet" href="https://developermen.com/assets/libs/jarallax/dist/jarallax.css">
  <link rel="stylesheet" href="https://developermen.com/assets/libs/highlightjs/styles/vs2015.css">
  <link rel="stylesheet" href="https://developermen.com/assets/libs/@fancyapps/fancybox/dist/jquery.fancybox.min.css">

  <!-- Map -->
  <link href='https://api.mapbox.com/mapbox-gl-js/v0.53.0/mapbox-gl.css' rel='stylesheet' />

  <!-- Theme CSS -->
  <link rel="stylesheet" href="https://developermen.com/assets/css/theme.min.css">
  <link rel="stylesheet" href="https://developermen.com/assets/css/styles.css">
  <script src="https://developermen.com/assets/libs/jquery/dist/jquery.min.js" charset="UTF-8"></script>


  <title>Error 404 &bull; Developer Men</title>
</head>

<body>
  <style media="screen">
    body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      width: 100%;
      height: 100%;
    }

    .contenido404 {
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translateX(-50%) translateY(-50%);
      font-family: "Montserrat";
      color: rgb(255, 255, 255);
      z-index: 1;
      text-align: center;
      width: 100%;
    }
  </style>
  <!-- CONTENT
  ================================================== -->
  <section class="section-border border-primary contenido404">
    <div class="container d-flex flex-column">
      <div class="row align-items-center justify-content-center no-gutters min-vh-100">
        <div class="col-8 col-md-5 col-lg-7 offset-md-1 order-md-2 mt-auto mt-md-0 pt-8 pb-4 py-md-11">
          <div id="LottieContainer"></div>
          <!-- Image -->
          <!-- <img src="https://developermen.com/assets/img/illustrations/Taken.svg" alt="..." class="img-fluid"> -->

        </div>
        <div class="col-12 col-md-6 col-lg-4 order-md-1 mb-auto mb-md-0 pb-8 py-md-11">

          <!-- Heading -->
          <h1 class="display-3 font-weight-bold text-center">
            Error <span style="color:#E5483F;">404</span>
          </h1>

          <!-- Text -->
          <p class="mb-5 text-justify text-white">
            Nos encontramos con un problema, pero no se preocupe, lo solucionaremos con seguridad.
            <div class="bd-callout bd-callout-danger bg-dark">
              <p class="h6 text-left text-muted">Verifica la URL:</p>
              <textarea name="name" rows="2" style="width:100%;background:transparent;border:none;color:#E56353;" disabled><?php $url = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH);
                                                                                                                            echo "$url"; ?></textarea>
              <!-- <p class="font-size-875 text-center mx-auto h-25">
                <strong>
                  <?php $url = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH);
                  echo "<span style='color:#E56353;'> $url </span>"; ?>
                </strong>
              </p> -->
            </div>
          </p>

          <!-- Link -->
          <div class="text-center">
            <a class="btn btn-primary btn-sm btn-block" href="/">
              <i class="fas fa-home"></i> Ir al sitio principal?
            </a>
          </div>

        </div>
      </div> <!-- / .row -->
    </div> <!-- / .container -->
  </section>

  <script>
    let max_particles = 1200;
    let particles = [];
    let frequency = 20;
    let init_num = max_particles;
    let max_time = frequency * max_particles;
    let time_to_recreate = false;
    const data = createCanvas()
    let tela = data[0];
    let canvas = data[1];

    // Enable repopolate
    setTimeout(function() {
      time_to_recreate = true;
    }.bind(this), max_time)

    // Popolate particles
    popolate(max_particles);

    class FishEgg {
      constructor(canvas) {
        const random = Math.random()
        this.progress = 0;
        this.canvas = canvas;
        // Set position
        this.x = ($(window).width() / 2) + (Math.random() * 300 - Math.random() * 300)
        this.y = ($(window).height() / 2) + (Math.random() * $(window).height() / 4 - Math.random() * $(window).height() / 4)
        // Get viewport size
        this.w = $(window).width()
        this.h = $(window).height()

        // Dimension
        this.radius = 12 + Math.random() * 6
        // Color
        this.color = "rgb(141, 59, 235)"
        // Setting
        this.fish_egg = {
          offset1: Math.random() > 0.5 ? 0.5 + Math.random() * 3 : 0.5 + Math.random() * -3,
          offset2: Math.random() > 0.5 ? 0.5 + Math.random() * 3 : 0.5 + Math.random() * -3,
          offset3: Math.random() > 0.5 ? 0.5 + Math.random() * 3 : 0.5 + Math.random() * -3,
          radius1: 0.5 + Math.random() * 5,
          radius2: 0.5 + Math.random() * 5,
          radius3: 0.5 + Math.random() * 5
        }
        this.variantx1 = Math.random() * 100
        this.variantx2 = Math.random() * 100
        this.varianty1 = Math.random() * 100
        this.varianty2 = Math.random() * 100
      }

      createCircle(x, y, r, c) {
        this.canvas.beginPath();
        this.canvas.fillStyle = c;
        this.canvas.arc(x, y, r, 0, Math.PI * 2, false);
        this.canvas.fill();
        this.canvas.closePath();
      }

      createEyes() {
        this.createCircle(this.x + this.fish_egg.offset2, this.y + this.fish_egg.offset2, this.fish_egg.radius2 + 4, "rgba(241, 242, 244, 0.06)")
        this.createCircle(this.x + this.fish_egg.offset3, this.y + this.fish_egg.offset3, this.fish_egg.radius3 + 2, "rgba(255, 204, 67, 0.08)")
        this.createCircle(this.x + (Math.random(this.progress / 350) * this.fish_egg.offset1), this.y + (Math.random(this.progress / 350) * this.fish_egg.offset1), this.fish_egg.radius1, "rgba(152, 19, 4, 0.19)")
      }

      render() {
        // Create inside parts
        this.createEyes()

        this.canvas.beginPath();
        let c = '130, 151, 180';
        let rad = this.canvas.createRadialGradient(this.x, this.y, this.radius, this.x, this.y, 1);
        rad.addColorStop(0, 'rgba(' + c + ',0.09)');
        rad.addColorStop(0.9, 'rgba(' + c + ',0)');
        this.canvas.lineWidth = Math.random() * 2.2;
        this.canvas.fillStyle = rad;
        this.canvas.arc(this.x, this.y, this.radius, 0, Math.PI * 2, false);
        this.canvas.fill();
        this.canvas.strokeStyle = "rgba(255, 255, 217, 0.05)";
        this.canvas.stroke();
        this.canvas.closePath();
      }

      move() {
        this.x += (Math.sin(this.progress / this.variantx1) * Math.cos(this.progress / this.variantx2)) / 8;
        this.y += (Math.sin(this.progress / this.varianty1) * Math.cos(this.progress / this.varianty2)) / 8;



        if (this.x < 0 || this.x > this.w - this.radius) {
          return false
        }

        if (this.y < 0 || this.y > this.h - this.radius) {
          return false
        }
        this.render()
        this.progress++
        return true
      }
    }

    class FishLarva {
      constructor(canvas, progress) {
        const random = Math.random()
        this.progress = 0;
        this.canvas = canvas;
        this.speed = 0.5 + random * 1.3

        this.x = ($(window).width() / 2) + (Math.random() * 200 - Math.random() * 200)
        this.y = ($(window).height() / 2) + (Math.random() * 200 - Math.random() * 200)

        this.s = 0.8 + Math.random() * 0.6;
        this.a = 0

        this.w = $(window).width()
        this.h = $(window).height()
        this.radius = random * 1.3
        this.color = "#f69a34"

        this.variantx1 = Math.random() * 1000
        this.variantx2 = Math.random() * 1000
        this.varianty1 = Math.random() * 1000
        this.varianty2 = Math.random() * 1000
      }

      render() {
        this.canvas.beginPath();
        this.canvas.arc(this.x, this.y, this.radius, 0, 2 * Math.PI);
        this.canvas.lineWidth = 2;
        this.canvas.fillStyle = this.color;
        this.canvas.fill();
        this.canvas.closePath();
      }

      move() {
        // this.x += (Math.sin(this.progress/this.variantx1)*Math.cos(this.progress/this.variantx2))/this.speed;
        // this.y += (Math.sin(this.progress/this.varianty1)*Math.cos(this.progress/this.varianty2))/this.speed;
        this.x += Math.cos(this.a) * this.s;
        this.y += Math.sin(this.a) * this.s;
        this.a += Math.random() * 0.8 - 0.4;
        if (this.x < 0 || this.x > this.w - this.radius) {
          return false
        }

        if (this.y < 0 || this.y > this.h - this.radius) {
          return false
        }
        this.render()
        this.progress++
        return true
      }
    }


    class FishLarvaEgg {
      constructor(canvas, progress) {
        const random = Math.random()
        this.progress = 0;
        this.canvas = canvas;
        this.speed = 0.5 + random * 0.2

        this.x = ($(window).width() / 2) + (Math.random() * 200 - Math.random() * 200)
        this.y = ($(window).height() / 2) + (Math.random() * 200 - Math.random() * 200)

        this.s = Math.random() * 1;
        this.a = 0

        this.w = $(window).width()
        this.h = $(window).height()
        this.radius = random * 0.8
        this.color = random > 0.8 ? "#82a0c4" : "#2E4765"

        this.variantx1 = Math.random() * 100
        this.variantx2 = Math.random() * 100
        this.varianty1 = Math.random() * 100
        this.varianty2 = Math.random() * 100
      }

      render() {
        this.canvas.beginPath();
        this.canvas.arc(this.x, this.y, this.radius, 0, 2 * Math.PI);
        this.canvas.lineWidth = 2;
        this.canvas.fillStyle = this.color;
        this.canvas.fill();
        this.canvas.closePath();
      }

      move() {
        this.x += Math.cos(this.a) * this.s;
        this.y += Math.sin(this.a) * this.s;
        this.a += Math.random() * 0.8 - 0.4;

        if (this.x < 0 || this.x > this.w - this.radius) {
          return false
        }

        if (this.y < 0 || this.y > this.h - this.radius) {
          return false
        }
        this.render()
        this.progress++
        return true
      }
    }

    class Paramecium {
      constructor(canvas) {
        const random = Math.random()
        this.progress = 0;
        this.canvas = canvas;
        // Set position
        this.x = ($(window).width() / 2) + (Math.random() * 300 - Math.random() * 300)
        this.y = ($(window).height() / 2) + (Math.random() * $(window).height() / 4 - Math.random() * $(window).height() / 4)
        // Get viewport size
        this.w = $(window).width()
        this.h = $(window).height()
        this.rotation = (random * 180) * Math.PI / 180
        // Dimension
        this.radius = 12 + Math.random() * 6
        // Color
        this.color = "rgba(255,255,255,1)"
        // Setting
        this.variantx1 = Math.random() * 100
        this.variantx2 = Math.random() * 100
        this.varianty1 = Math.random() * 100
        this.varianty2 = Math.random() * 100
      }

      createOval(x, y, w, h) {
        var kappa = .5522848,
          ox = (w / 2) * kappa, // control point offset horizontal
          oy = (h / 2) * kappa, // control point offset vertical
          xe = x + w, // x-end
          ye = y + h, // y-end
          xm = x + w / 2, // x-middle
          ym = y + h / 2; // y-middle

        this.canvas.save();

        this.canvas.translate(this.w / 2, this.h / 2);

        // Rotate 1 degree
        this.canvas.rotate(this.rotation);

        // Move registration point back to the top left corner of canvas
        this.canvas.translate(-this.w / 2, -this.h / 2);

        this.canvas.beginPath();
        this.canvas.moveTo(x, ym);
        this.canvas.quadraticCurveTo(x, y, xm, y);
        this.canvas.quadraticCurveTo(xe, y, xe, ym);
        this.canvas.quadraticCurveTo(xe, ye, xm, ye);
        this.canvas.quadraticCurveTo(x, ye, x, ym);

        this.canvas.strokeStyle = 1;
        this.canvas.fillStyle = "rgba(255,255,255,0.01)";
        this.canvas.fill();
        this.canvas.stroke();
        this.canvas.restore();
      }

      render() {
        // Create inside parts
        this.createOval(this.x, this.y, 12, 4)
      }

      move() {
        this.x += (Math.sin(this.progress / this.variantx1) * Math.cos(this.progress / this.variantx2)) / 4;
        this.y += (Math.sin(this.progress / this.varianty1) * Math.cos(this.progress / this.varianty2)) / 4;

        if (this.x < 0 || this.x > this.w - this.radius) {
          return false
        }

        if (this.y < 0 || this.y > this.h - this.radius) {
          return false
        }
        this.render()
        this.progress++
        return true
      }
    }

    /*
     * Function to create canvas
     */
    function createCanvas() {
      let tela = document.createElement('canvas');
      tela.width = $(window).width();
      tela.height = $(window).height();
      $("body").append(tela);
      let canvas = tela.getContext('2d');
      return [tela, canvas]
    }

    /*
     * Function to clear layer canvas
     * @num:number number of particles
     */
    function popolate(num) {
      for (var i = 0; i < num; i++) {
        setTimeout(
          function(x) {
            return function() {
              let random = Math.random()
              // ------------------------------------
              // Set type of planktom
              let type = new FishLarva(canvas)
              if (!time_to_recreate) {
                if (random > .97) type = new FishEgg(canvas);
                if (random < .1 && random > 0) type = new Paramecium(canvas)
              }
              if (random > .1 && random < .8) type = new FishLarvaEgg(canvas)

              // if(random < .1) this.type  = "bryozoan"
              // ------------------------------------
              // Add particle
              particles.push(type)
            };
          }(i), frequency * i);
      }
      return particles.length
    }

    /*
     * Function to clear layer canvas
     */
    function clear() {
      let grd = canvas.createRadialGradient(tela.width / 2, tela.height / 2, 0, tela.width / 2, tela.height / 2, tela.width);
      grd.addColorStop(0, "rgba(25,25,54,0.12)");
      grd.addColorStop(1, "rgba(0,0,20,0.01)");
      // Fill with gradient
      canvas.fillStyle = grd;
      canvas.fillRect(0, 0, tela.width, tela.height);
    }

    /*
     * Function to update particles in canvas
     */
    function update() {
      clear();
      particles = particles.filter(function(p) {
        return p.move()
      })
      // Recreate particles
      if (time_to_recreate) {
        if (particles.length < init_num) {
          popolate(1)
        }
      }
      requestAnimationFrame(update.bind(this))
    }
    // Update canvas
    update()
  </script>

  <!-- JAVASCRIPT
    ================================================== -->
  <!-- Libs JS -->
  <script src="https://developermen.com/assets/libs/jquery/dist/jquery.min.js"></script>
  <script src="https://developermen.com/assets/libs/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://developermen.com/assets/libs/flickity/dist/flickity.pkgd.min.js"></script>
  <script src="https://developermen.com/assets/libs/flickity-fade/flickity-fade.js"></script>
  <script src="https://developermen.com/assets/libs/aos/dist/aos.js"></script>
  <script src="https://developermen.com/assets/libs/smooth-scroll/dist/smooth-scroll.min.js"></script>
  <script src="https://developermen.com/assets/libs/jarallax/dist/jarallax.min.js"></script>
  <script src="https://developermen.com/assets/libs/jarallax/dist/jarallax-video.min.js"></script>
  <script src="https://developermen.com/assets/libs/jarallax/dist/jarallax-element.min.js"></script>
  <script src="https://developermen.com/assets/libs/typed.js/lib/typed.min.js"></script>
  <script src="https://developermen.com/assets/libs/countup.js/dist/countUp.min.js"></script>
  <script src="https://developermen.com/assets/libs/highlightjs/highlight.pack.min.js"></script>
  <script src="https://developermen.com/assets/libs/@fancyapps/fancybox/dist/jquery.fancybox.min.js"></script>
  <script src="https://developermen.com/assets/js/lottie.min.js?scripts"></script>

  <!-- Map -->
  <script src='https://api.mapbox.com/mapbox-gl-js/v0.53.0/mapbox-gl.js'></script>

  <!-- Theme JS -->
  <script src="https://developermen.com/assets/js/theme.min.js"></script>
  <script src="https://developermen.com/assets/js/main.js"></script>

  <script>
    /**Params: id, path, loop, speed, autoplay*/
    lottieImage('LottieContainer', 'https://developermen.com/assets/img/lotties/404-animation.json','',false);
  </script>
</body>

</html>